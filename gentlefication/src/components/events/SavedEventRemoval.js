import React, { useState, useEffect } from 'react';
import { useHistory } from "react-router";
import { getSavedEventById, savedEventRemoval } from '../../modules/EventsManager';

export const handleDelete = id => {}
